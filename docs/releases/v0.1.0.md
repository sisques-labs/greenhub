# Release Management

This document defines the scope and boundaries of a project version.
Nothing is developed unless it is explicitly included in a version.

---

## Version: v0.1.0 — Core Cultivation MVP

### Status
- 🚧 In Progress

### Objective
> Establish the core cultivation domain, enabling reliable modeling and management of plants and their physical containers (pots) and their relationships, without including care actions, sensors, or automation. This version answers the question: *Can I represent what plants I have and where they are?*

---

## Scope (What’s Included)

### Backend (GraphQL)
- **Domain**
  - Plant aggregate: creation, name, species, planted date, notes (optional)
  - Plant status: `ACTIVE | ARCHIVED`
  - Container aggregate: creation, type, name, location, metadata (optional)
  - Relationship: assign/remove plant to/from container (1..N)
- **Application / Use Cases**
  - Create plant
  - Create container
  - Assign plant to container
  - Remove plant from container
  - Archive plant
- **Infrastructure**
  - TypeORM for write model (Postgres)
  - MongoDB for read projections (CQRS)
  - Multi-tenant support (DB or schema per tenant)
  - Soft delete technical (not exposed in API)
- **Read Model / Projections**
  - `PlantView`: id, name, species, status, container (id + name), location
  - `ContainerView`: id, name, type, location, plants[] (id + name)
- **GraphQL API**
  - **Mutations**
    - `createPlant(input: CreatePlantInput): Plant`
    - `archivePlant(id: ID!): Plant`
    - `createContainer(input: CreateContainerInput): Container`
    - `assignPlantToContainer(plantId: ID!, containerId: ID!): Plant`
    - `removePlantFromContainer(plantId: ID!, containerId: ID!): Plant`
  - **Queries**
    - `plants: [Plant]`
    - `containers: [Container]`
    - `container(id: ID!): Container`
- **Domain Events**
  - `PlantCreated`
  - `PlantArchived`
  - `PlantAssignedToContainer`
  - `PlantRemovedFromContainer`
  - `ContainerCreated`

### Frontend
- **Views**
  - List of plants
  - List of containers
  - Container detail showing assigned plants
- **Actions**
  - Create plant
  - Create container
  - Assign / remove plant from container
  - Archive plant
- **GraphQL Integration**
  - Consume backend GraphQL API
  - Handle queries and mutations for listed actions
- **UI / UX**
  - Basic functional interface (visuals minimal, focus on functionality)
  - No dashboards, charts, or advanced filtering

---

## Explicitly Out of Scope
- Watering or care actions
- Sensors or automation
- Advanced agronomic rules
- History, notifications, or AI
- Photo management
- Calendars or schedules
- Import/export functionality
- Complex editing of entities

---

## Acceptance Criteria
This version is considered complete when:
- [ ] Backend: aggregates, events, read models, GraphQL API functional
- [ ] Frontend: views allow listing, creating, assigning, removing, archiving plants/containers via GraphQL
- [ ] Multi-tenant support works for both backend and frontend
- [ ] Soft delete / archive is correctly handled in backend and reflected in frontend

---

## Technical Notes / Decisions
- Domain models decoupled from persistence (TypeORM + CQRS)
- Soft delete implemented technically, not exposed
- Multi-tenant architecture applied (DB or schema per tenant)
- GraphQL used as single API layer for frontend
- Minimal UI for MVP; focus on functionality

---

## Follow-ups / Deferred Items
- Watering tracking
- Sensor integration
- Automation rules
- Event-driven notifications
- Photos and media per plant
- Advanced queries or dashboards
- UI/UX improvements

---

## Changelog (Filled on Release)

### Added
- Backend aggregates, read models, GraphQL API
- Frontend views for listing and managing plants/containers
- Assign/remove and archive functionality via GraphQL
- Basic multi-tenant support

### Changed
- N/A (first release)

### Fixed
- N/A (first release)

---

## Release Tag
- `v0.1.0`

## Release Date
- YYYY-MM-DD
