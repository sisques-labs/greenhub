/**
 * Default options for items per page selector in paginated tables
 */
export const DEFAULT_PER_PAGE_OPTIONS = [10, 25, 50, 100] as const;

/**
 * Default number of items per page
 */
export const DEFAULT_PER_PAGE = 10;
